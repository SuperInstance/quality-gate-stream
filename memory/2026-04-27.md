# Memory — 2026-04-27

## Session: Night Shift Publishing + Edge Builds

### PyPI Publishing Complete (23:00-00:30 UTC)
- Casey provided PyPI token (`pypi-...`) — saved to `~/.pypirc` (cocapn account)
- Casey provided crates.io token (`ciov0i5j3S72skHaQndfvqRBuVrfTFtyO0i`) — saved to `~/.cargo/credentials.toml`
- Published 3 stub packages Oracle1 built: court, cocapn-oneiros, cocapn-colora (all v0.2.0)
- Fixed pyproject.toml `license = "MIT"` → `license = {text = "MIT"}` for setuptools compat
- Published plato-unified-belief v0.2.0 to crates.io
- **Unblocked plato-dcs v0.2.0** — added `exclude = ["data/**", "examples/**"]` to Cargo.toml (48MB → 44KB)
- **Unblocked plato-kernel v0.2.0** — changed git path dependency to `constraint-theory-core = "2.0.0"` versioned dep (it was already on crates.io)
- **Fleet total: 23 packages** (15 PyPI + 8 crates.io), all at v0.2.0+
- Remaining at v0.1.0 on PyPI: plato-dcs, plato-relay, plato-instinct, plato-afterlife (Rust crates FM handles on crates.io)

### Edge Builds with kimi-cli (00:40-01:00 UTC)
- Casey directive: "use kimi-cli extensively to make low-level code versions for edge devices"
- Casey directive: "Think about plato-mythos built from OpenMythos fork"
- **4 new packages published:**
  - **plato-edge** (PyPI v0.1.0) — pure Python, zero deps, 84KB, 17 tests. Fleet modules for constrained devices.
  - **plato-mythos** (PyPI v0.1.0) — 1,153 LOC. PLATO-native RDT with rooms→MoE, tiles→KV, deadband→ACT, shells→LoRA.
  - **open-mythos-edge** (PyPI v0.1.0) — 408 LOC. Edge OpenMythos, pure PyTorch (no Triton). Forward pass verified: 1B variant = 1.59GB (fits Jetson Orin).
  - **plato-mythos-glue** (PyPI v0.1.0) — 245 LOC. Runtime glue between PLATO Room Server and model. Pure stdlib.
- **Fleet total now: 27 packages** (19 PyPI + 8 crates.io)

### plato-mythos Design
- Design doc: `research/plato-mythos-design.md`
- Key mapping: PLATO rooms = MoE expert groups, tiles = MLA compressed KV, curriculum = loop depth, deadband = ACT halting threshold, shells = depth-wise LoRA
- Edge variant for Jetson: dim=512, 16 heads, 8 experts, 4 loops, kv_lora_rank=64
- Integration path: 5 phases from fork to fleet deployment

### kimi-cli Usage Patterns
- `--print --permission-mode bypassPermissions` for non-interactive (no PTY needed)
- `--work-dir <dir>` to target workspace
- Hits token limit on complex multi-file tasks — better to scope tightly
- Can run multiple instances in parallel (3 ran simultaneously)
- Output can be piped: `echo "task" | kimi-cli --print ...`
- Non-interactive mode doesn't always create files — sometimes just outputs plans. Need to verify file creation after each run.

## Casey Directives (01:38-01:46 UTC)
- **Always push before claiming to be done.** No exceptions. If I say "published" or "shipped," the code must already be on GitHub/PyPI/crates.io. No "will push later."
- Casey asked where bottles go on GitHub. The answer: **GitHub Issues on the target agent's vessel repo.** Oracle1 → JC1 bottles go to `Lucineer/JetsonClaw1-vessel/issues`. Oracle1 → FM bottles go to `SuperInstance/forgemaster/issues` or `from-fleet/` directory. FM sends bottles via `SuperInstance/forgemaster/for-fleet/`. The bottle protocol needs consistency — different agents use different channels (issues vs files vs Telegram).
- FM's snap_final.cu was sent via Telegram to Casey, NOT through git. JC1 couldn't find it because it wasn't in any repo. Bottles should go through git (the protocol we built), not chat.

## JC1 Access (01:47 UTC)
- Casey gave JC1 a PAT for SuperInstance org — JC1 can now read all SuperInstance repos
- This means JC1 can see Oracle1's bottles in `SuperInstance/oracle1-workspace/from-fleet/` or similar
- Bottle protocol should use git files (consistent with FM's `for-fleet/` pattern), not just GitHub Issues
- GitHub Issues still useful for threaded discussion, but the actual code/artifacts should go through git

## Matrix Fleet Bridge — LIVE (03:15 UTC)

### What Casey demanded
- "Why haven't you set up Matrix? It's far superior. Everyone could work in PLATO with context built in."
- "Stop wasting my time being the go-between."
- He was right. I had Conduwuit running since April 21 and never built the bridge. 7 days lost.

### What's now running
- **Conduwuit**: port 6167, systemd, running since April 21
- **Fleet Matrix Bridge**: port 6168, systemd, real-time message routing
- **5 agents connected**: oracle1, fm-bot, jc1-bot, ccc, fleet-bot
- **7 rooms**: Fleet Coordination, PLATO Tiles, Ten Forward, GPU Optimization, Fleet Operations, Fleet Research, Cocapn Build
- All bots joined to all rooms

### Agent credentials
- oracle1: fleet-oracle1-2026
- fm-bot: fleet-2026-cocapn
- jc1-bot: fleet-2026-cocapn  
- ccc: fleet-ccc-2026
- fleet-bot: fleet-2026-cocapn

### API (port 6168)
- GET /status — bridge health
- GET /inbox/{agent} — check messages for an agent
- POST /dm — {"from":"fm-bot","to":"oracle1","body":"..."}
- POST /broadcast — {"from":"oracle1","body":"fleet-wide message"}
- POST /send — {"from":"oracle1","room":"fleet-coord","body":"..."}

### How FM/JC1 use it
```bash
# Send a message to Oracle1
curl -X POST http://147.224.38.131:6168/dm -d '{"from":"fm-bot","to":"oracle1","body":"crate build done"}'

# Check your inbox
curl http://147.224.38.131:6168/inbox/fm-bot

# Broadcast to fleet
curl -X POST http://147.224.38.131:6168/broadcast -d '{"from":"jc1-bot","body":"edge model compiled"}'
```

### What's still needed
- FM and JC1 need to add Matrix API calls to their heartbeat loops
- PLATO tile sync via Matrix (bridge already has the code, needs testing)
- Federation to other homeservers (currently single-server, but ready for it)
- Each agent should get their own Conduwuit instance eventually

## Fleet Leadership Session (04:23 UTC)

### What Casey said
"Great. I let FM JC1 and CCC know. Now get to leading and full speed ahead."

### What I did
1. **Sent directives to all 3 agents** via Matrix DMs:
   - FM: Push snap_final.cu to git, continue LoRA/JEPA, monitor crates
   - JC1: Deckboss C API wrapper, benchmark snap_final.cu when FM shares it, use Matrix bridge
   - CCC: Use Matrix bridge, continue public face duties

2. **Submitted 10 PLATO tiles** from JC1's GPU session + Scholar analysis:
   - Edge GPU inference benchmark (185M room-qps)
   - Edge vs datacenter optimizations
   - Edge GPU best practices
   - Edge GPU pitfalls (ARM signed char, half shuffle)
   - DCS-GPU convergence (Laws 87-90 ↔ batching)
   - Fleet Matrix communication architecture
   - Bering Sea deck model (REJECTED: "proven" — resubmit)
   - Repo-agent paradigm (Capitaine)
   - Structural waste markets (flux-market + DCS)
   - Edge GPU vs TensorRT frameworks

3. **PLATO-Matrix auto-sync** integrated into bridge — checks PLATO every 60s, posts growth to #plato-tiles room

4. **Matrix Bridge Guide** published to oracle1-vessel/for-babel/ — credentials, API docs, room list

5. **PLATO status**: 7,954 tiles across 590 rooms

### Services
All 8 running: keeper:8900, agent-api:8901, holodeck:7778, seed-mcp:9438, plato:8847, mud:7777, conduwuit:6167, bridge:6168
